import Card from './Card';

// Displays the header for the website
const Header = () => {
    return(
        <header>
            <Card>
                <p>Online Pokedex</p>
            </Card>
        </header>
    );
}

export default Header;