import styled from 'styled-components';

// Wrapper component for the grid system on the home page
const GridWrapper = styled.div`
    display: grid;
    grid-template-columns: 1fr 1fr 1fr 1fr 1fr;
    grid-gap: 100px;
`;

export default GridWrapper;