import styled from 'styled-components';

// Displays the card that shows the name of each Pokemon
const PokemonCard = styled.div`
    width: 200px;
    length: 200px;
    color: #ffcb05; /* hex value for Pokemon logo yellow */
    background-color: #2a75bb; /* hex value for Pokemon logo light blue */
    aspect-ratio: 1;
`;

export default PokemonCard;